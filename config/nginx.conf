worker_processes auto;

events {
  worker_connections 1024;
}

stream {
  upstream xray_vless {
    server xray:443;
  }

  server {
    listen 8443;
    proxy_pass xray_vless;
    proxy_connect_timeout 10s;
    proxy_timeout 1h;
  }
}
